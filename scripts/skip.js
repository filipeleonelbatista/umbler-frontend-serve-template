
process.exit(0);

// Colocar a variavel de ambiente SKIP_BUILD com o valor 1. caso não funcione basta deixar como está
// if (process.env.SKIP_BUILD) {
//     process.exit(0);
// } else {
//     process.exit(1);
// }